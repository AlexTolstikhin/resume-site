/** Personal details and contact info shown in the resume header. */
export type Profile = {
  first: string;
  last: string;
  title: string;
  location: string;
  phone: string;
  email: string;
  linkedin: string;
  linkedinLabel: string;
  bio: string;
};
